apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-daily
  namespace: devops-daily
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: devops-daily.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: devops-daily
                port:
                  number: 80
